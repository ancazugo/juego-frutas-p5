function setup() {
    createCanvas(400, 400);
}

function draw() {
    background(220);
    fill(154, 205, 50); // Yellow-green color
    ellipse(width / 2, height / 2, 200, 200); // Draws a circle at the center
}